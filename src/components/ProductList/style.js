import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  searchInput: {
    height: 40,
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 8,
    paddingHorizontal: 10,
    // marginBottom: 8,
    // marginTop:6,
    margin:12,
  },

});
